# DESIGN_TOKENS (APPROVED 2026-09-20 — UI_MASTER_PLAN)

> Values below are approved for all Thymeleaf templates. Keep AA contrast on text.

- Font: system stack (`system-ui, -apple-system, "Segoe UI", Roboto, sans-serif`); base 16px;
  scale 0.875 / 1 / 1.25 / 1.5 / 2rem; line-height 1.5.
- Ink `#1a1a1a`; muted `#5b6472`; border `#d7dbe3`; surface `#f4f5fb`; page `#ffffff`.
- Primary `#4338ca`, hover `#3730a3`, on-primary `#ffffff`. Focus ring `#1a73e8`.
- Error text `#b00020`. Radius: cards 10px, inputs/buttons 6px. Spacing: 4/8/16/24/32.
- Status pills (bg / text): Pending `#fef3c7`/`#92400e`; Approved `#dbeafe`/`#1d4ed8`;
  Rejected `#fee2e2`/`#b91c1c`; Active `#dcfce7`/`#15803d`; Completed `#e2e8f0`/`#475569`.
- Layout: content max-width 56rem (72rem on dashboard/lists via `.wide`); 44px min touch targets.
